-- Create places table
CREATE TABLE places (
covidsafe(> place_id TEXT NOT NULL,
covidsafe(> popular_times_response TEXT);

-- Create ratings table
CREATE TABLE ratings (
covidsafe(> place_id TEXT NOT NULL,
covidsafe(> rating INTEGER);